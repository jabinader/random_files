import { FormEditingTemplateDirective } from './form-editing-template.directive';

describe('FormEditingTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new FormEditingTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
